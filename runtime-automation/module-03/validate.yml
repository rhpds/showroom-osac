---
# Module 3: Catalog Items and Workloads — validate tasks
# Generate with: /ftl:rhdp-lab-validator
- name: Module 03 - Validate placeholder
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Placeholder
      ansible.builtin.debug:
        msg: "Validate tasks not yet generated. Run /ftl:rhdp-lab-validator to create."
