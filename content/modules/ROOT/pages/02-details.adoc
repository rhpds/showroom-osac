= Workshop Details
:toc:
:toc-placement: preamble
:icons: font

include::ROOT:partial$_attributes.adoc[]

Read through this page before starting Module 1. It covers timing, what you need to know coming in, and how to reach every part of your lab environment. Five minutes here will save you time during the hands-on exercises.

== Timing and Schedule

=== Full Workshop (90 minutes)

* *Module 1*: Exploring the OSAC Platform (30 minutes)
* *Module 2*: Managing Tenants (30 minutes)
* *Module 3*: Catalog Items and Workloads (30 minutes)

Each module builds on the previous one, but they are structured so that a knowledgeable platform engineer can also pick up at Module 2 or Module 3 independently. No formal breaks are scheduled — pause between modules as needed.

=== Abbreviated Workshop (60 minutes)

* *Module 1*: Exploring the OSAC Platform (20 minutes)
* *Module 2*: Managing Tenants (25 minutes)
* *Module 3*: Catalog Items and Workloads — core exercises only (15 minutes)

Skip the verification and troubleshooting steps in each module when running the abbreviated schedule.

== Prerequisites

=== Knowledge

You should be comfortable with:

* OpenShift concepts — projects, namespaces, operators, and the `oc` CLI
* Kubernetes resource types, including Custom Resource Definitions (CRDs)
* Reading and editing YAML
* Linux command-line usage at an intermediate level

You do not need prior experience with Ceph storage, Keycloak, Kafka, or the OSAC platform. Each component is introduced in context as you encounter it.

=== Software Requirements

All hands-on work runs inside the browser-based terminal Showroom provides. You do not need to install anything locally to complete the workshop.

If you prefer your own terminal, you need:

* *OpenShift CLI (`oc`)*: {ocp_version} or later — available at https://mirror.openshift.com/pub/openshift-v4/clients/ocp/latest/[mirror.openshift.com^]
* *osac CLI*: pre-installed in the lab environment (bastion host)
* *Web browser*: Chrome, Firefox, Safari, or Edge (current release)

== Environment Access

Your lab environment provides:

* A multi-node Red Hat OpenShift {ocp_version} cluster
* The OSAC platform pre-installed in the `{osac_namespace}` namespace
* A bastion host with the `osac` CLI and `oc` CLI already configured
* Pre-deployed Keycloak, Kafka, and Ceph storage components

=== OpenShift Console

Open the OpenShift web console in a new browser tab:

----
{openshift_console_url}
----

Log in with these credentials:

[cols="1,2",options="header"]
|===
|Field |Value
|Username |`{admin_user}`
|Password |`{admin_password}`
|===

=== OSAC Platform UI

The OSAC dashboard is available at:

----
{osac_ui_url}
----

Use the same admin credentials as the OpenShift console.

=== Keycloak Administration Console

Keycloak provides identity management for the OSAC platform. Open it at:

----
{keycloak_url}
----

You review Keycloak configuration during Module 1. Log in with the admin credentials shown in your lab details panel.

=== Bastion Host (SSH)

The bastion host gives you CLI access to the cluster. Connect from your local terminal:

[source,bash,role="execute"]
----
ssh {bastion_ssh_user_name}@{bastion_public_hostname}
----

When prompted, enter the password: `{bastion_ssh_password}`

Alternatively, click the *Terminal* tab in the Showroom sidebar — it connects you to the bastion automatically with no SSH setup required.

== Pre-Workshop Checklist

Work through this list before opening Module 1:

* [ ] *OpenShift Console accessible* — Log in at `{openshift_console_url}` and confirm the cluster overview loads
* [ ] *OSAC UI accessible* — Open `{osac_ui_url}` and confirm the dashboard appears
* [ ] *Terminal tab open* — Click *Terminal* in the Showroom sidebar
* [ ] *`oc` CLI verified* — Run `oc whoami` and confirm your username returns
* [ ] *osac CLI available* — Run `osac --version` and confirm a version string returns
* [ ] *OSAC namespace healthy* — Run `oc get pods -n {osac_namespace}` and confirm pods are Running

== Environment Verification

Run these commands in the Terminal tab to confirm the lab is ready.

Confirm your OpenShift identity:

[source,bash,role="execute"]
----
oc whoami
----

Expected output: `{admin_user}`

Check the overall cluster version:

[source,bash,role="execute"]
----
oc version
----

Verify the OSAC operator pod is running:

[source,bash,role="execute"]
----
oc get pods -n {osac_namespace} -l app.kubernetes.io/name=osac-operator
----

All pods should show `Running`. If they show `Pending`, wait 60 seconds and retry.

Check the full set of OSAC platform pods:

[source,bash,role="execute"]
----
oc get pods -n {osac_namespace}
----

You should see pods for the OSAC operator, fulfillment service, Keycloak, and Kafka. All should be `Running` or `Completed`.

Confirm the osac CLI is available and configured:

[source,bash,role="execute"]
----
osac --version
----

== Troubleshooting

=== Cannot Log In to the OpenShift Console

Confirm you are using the correct credentials:

* *Username*: `{admin_user}`
* *Password*: `{admin_password}`

Try an incognito or private browser window to rule out a cached session. If the login page does not load, verify the URL is exactly `{openshift_console_url}`.

=== OSAC UI Does Not Load

Check that the ingress route exists in the `{osac_namespace}` namespace:

[source,bash,role="execute"]
----
oc get route -n {osac_namespace}
----

If the route is missing, the OSAC operator may still be initializing. Check pod status:

[source,bash,role="execute"]
----
oc get pods -n {osac_namespace}
----

Wait until all pods are `Running`, then refresh the OSAC UI URL.

=== SSH Connection Refused to Bastion

Verify the hostname and username:

[source,bash,role="execute"]
----
ssh {bastion_ssh_user_name}@{bastion_public_hostname}
----

If the connection is refused or times out, use the *Terminal* tab in Showroom instead — it provides an equivalent shell session without requiring direct SSH access from your machine.

=== Pods Are Pending or in CrashLoopBackOff

Inspect recent events in the OSAC namespace:

[source,bash,role="execute"]
----
oc get events -n {osac_namespace} --sort-by='.lastTimestamp' | tail -20
----

Share the output with your instructor. Common causes include Ceph storage not yet provisioned or resource quota limits on the cluster.

=== osac CLI Not Found

The `osac` binary is pre-installed on the bastion. If the command is not found, confirm you are in the bastion shell rather than your local terminal:

[source,bash,role="execute"]
----
echo $HOSTNAME
which osac
----

If `osac` is absent from the bastion, ask your instructor — the binary path may need to be added to `PATH`.

== Additional Resources

* https://docs.openshift.com/container-platform/{ocp_version}/welcome/index.html[Red Hat OpenShift {ocp_version} Documentation^]
* https://access.redhat.com/documentation/en-us/red_hat_openshift_data_foundation/[Red Hat OpenShift Data Foundation Documentation^]
* https://www.keycloak.org/documentation[Keycloak Documentation^]
* https://access.redhat.com/[Red Hat Customer Portal^]
